/** The three conditions, in display order.
 *  Lives apart from scoreAll.js so modules can import it without pulling in the
 *  rule JSON — which keeps the plain-node verification script working. */
export const CONDITION_IDS = ['diabetes', 'hypertension', 'cardiovascular'];
